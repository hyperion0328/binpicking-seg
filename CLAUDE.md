# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

빈피킹(로봇이 상자에서 부품을 집는 작업)용으로, 작고 얇은 체결부품(나사·볼트·너트·와셔)을
공개 데이터에서 **인스턴스 분할(segmentation)** 하는 프로젝트. 겹쳐 쌓인 더미에서 개체 경계를
픽셀 단위로 갈라내는 것이 목표이고, **SAM** 을 마스크 보충과 검증 대상으로 함께 쓴다.

- 대외용(쉬운 말) 기획서: `docs/proposal.html` ← **2차**
- 실측 누적: `docs/experiments.md` (E1 전처리 · E2 SAM 검증 · E3 채우기), 최종 정리: `docs/results.md` (아직 없음)
- **1차 프로젝트 전체는 `docs/v1/`** (기획·실험기록·결과·발표자료)

**이 프로젝트의 산출물은 코드가 아니라 "왜 이 선택인가"에 대한 답이다.** 숫자
하나를 바꾸기 전에 그 숫자가 어느 문서의 어느 결론을 무효화하는지 먼저 본다.

## 틀리기 쉬운 전제 — 먼저 확인할 것

**1차 결론을 2차 데이터에 그대로 인용하면 틀린다.** `docs/v1/results.md` 의 D1~D10 은
MVTec Screws / NPU-BOLT / PCBsmdComponents 기준이고, 2차는 데이터셋이 **완전히 교체**됐다.
수치는 못 쓰고, **방법론적 결론만 유효**하다 — 특히 "다운스케일로 만든 곡선은 진짜 소형
객체 성능의 낙관적 상한이다"는 2차 설계의 출발점이다.

**1차의 D6(“마스크 라벨이 없어 seg 를 접는다”)은 무효다.** 2차 데이터에는 마스크가 있다
(전체 33,435개 중 29,862개, 89.3%). "이 프로젝트엔 마스크가 없다"고 다시 말하면 틀린다.

**`rf-screw-nut-bolt` 는 종횡비가 왜곡돼 있다 — 보정 전에 학습하지 말 것.**
너트 박스의 가로세로비 중앙값이 **0.59**(원형이면 1.0). 볼트 0.64 / 나사 0.62 로
**세 클래스 모두 같은 값**이고, 이미지별 편차도 거의 없다(p10 0.575 ~ p90 0.611).
Roboflow 의 `Stretch to 512×512` 전처리가 원본 16:9 를 정사각형으로 늘린 결과다.
→ **512×288 로 복원**하고 주석 y 좌표를 0.5625배 한다. 모양을 배우는 seg 에서는 필수.

**`rf-screw-nut-bolt` 는 사진 한 장에 클래스가 하나뿐이다**(bolt 500장 / nut 500장 /
screw 300장 = 1,300장). 클래스 혼동을 잴 수 없다. **클래스별 AP 는 `rf-arg-fixings3` 와
`rf-nuts-and-bolts` 에서만 의미가 있다.** 이건 XYZ-IBD 를 뺀 이유와 같은 구조다.

**마스크 결측은 washer 만이 아니고, 전량 `rf-arg-fixings3` 다.** 3,850개(bolt 777 / nut 1,449 /
screw 410 / washer 1,214)가 한 세트에 몰려 있다. washer 를 빼면 안 되는 이유는 그대로다 —
`rf-arg-fixings3` **232장 중 138장(59%)** 에 washer 가 실제로 찍혀 있다.
**검증 후 채웠다**(E2·E3) — `sam_b`, train 에만, 2,536개(97.3% 통과), `pseudo: true` 표시.

**`rf-arg-fixings3` 의 정답 마스크는 꼭짓점 9개짜리 거친 폴리곤이다.** SAM 과의 IoU 0.700 은
SAM 이 못한 게 아니라 정답이 거친 것이다(정밀도 0.97 / 재현율 0.70 / 면적비 0.75, 눈으로 확인).
마스크 촘촘함은 세트마다 다르다 — arg-fixings3 9 / nuts-and-bolts 14 / fasteners 29~93 /
screw-nut-bolt 화소 단위. **mask AP 를 소스 합산으로 보고하면 상한을 가장 거친 정답이 정한다.**

**정답 washer 마스크는 100% 가 구멍을 메운 원판이다.** SAM 은 39% 를 도넛으로 낸다.
물리적으론 SAM 이 맞지만 한 클래스에 두 관례가 섞이면 안 되므로 **구멍을 메워** 넣는다(D17).

**"클래스가 없으면 사진에도 없다" — 사용자가 눈으로 확인한 사실이다.**
세트마다 다루는 부품 종류가 다른데(screw-nut-bolt 에 washer 없음, fasteners 에 screw·washer 없음),
**해당 부품이 물리적으로도 안 찍혀 있다.** 그래서 합쳐도 라벨 누락으로 인한 오탐 벌점이 없다.
라벨 통계만으로는 알 수 없던 사실이라, 새 데이터를 합칠 때도 같은 확인을 거쳐야 한다.

**대조군은 24장이 아니라 8장이다.** `rf-arg-fixings3` 640×480 그룹 24장 중
**16장(`env*`)은 부품이 없는 배경**이고, 472개 인스턴스는 나머지 8장(`Fixings*`)에 몰려 있다.
AP 는 8장에서만 나오고, 배경 16장은 **원거리 장면에서의 오탐 측정용**이다.

**주석 0개 이미지는 라벨 누락이 아니라 배경 사진이다.** 공구만 있거나 부품이 아예 없는
사진이며(사용자 확인), 드라이버·육각렌치는 볼트와 닮아 **오탐을 줄이는 하드 네거티브**다.
지우지 말 것. 현재 24장(전체의 1.3%)으로 오히려 적은 편이다.

**네 세트 모두 COCO `categories` 에 `id=0` 자리표시자가 있다**(`screw-nut-bolt`,
`Bolt-Nut-Screw-Washer`, `Nuts-and-Bolts`, `bolts-nuts`, 전부 `supercategory=none`, 인스턴스 0개).
안 빼면 인스턴스 0개 클래스가 낀다. **클래스명 대소문자도 섞여 있다**(`Bolt`/`bolt`,
`Washer`/`washer`) — 그대로 합치면 8클래스가 된다. 소문자 4종으로 통일한다.

**객체 크기 정의는 하나로 통일한다 — 외접 축정렬 박스의 `sqrt(area)`.**
"변대비"는 `sqrt(area) / sqrt(W*H)`. 새로 재는 값도 이 정의로 맞춘다.

## 실험 프로토콜 — 어기면 축이 섞인다

- **기준 해상도는 640**이다(1차의 1024 아님). 근거는 대조군 하나다 — imgsz 640 에서
  대조군(640×480)이 **1.00배로 리샘플링을 전혀 안 겪는다**. 1024 면 1.60배 확대(보간 뭉개짐),
  512 면 0.80배 축소(안티앨리어싱으로 매끈해짐)라 **양쪽 다 대조군의 "자연스러움"을 훼손**한다.
  비용은 `rf-screw-nut-bolt` 1,300장이 1.25배 확대되는 것뿐이고, 학습 자료의 확대는
  기준자인 대조군 훼손보다 가볍다.
- **다운스케일 축은 640 / 320 / 160.** 전부 stride 32 의 배수다. 네 번째 점이 필요하면
  **80이 아니라 96 또는 64**(80은 32로 안 나눠떨어진다).
- **레터박스 고정, stretch 금지.** `rf-screw-nut-bolt` 가 stretch 로 망가진 상태이고
  지금 그걸 되돌리는 중이다. 패딩은 114 회색, ultralytics 가 로딩 시점에 적용하게 둔다.
- **다운스케일은 이미지를 복제 저장하지 않고 `imgsz` 로 준다.** 리샘플링이 한 번만 일어난다.
- **평가 imgsz 는 학습 imgsz 와 같게 맞춘다.**
- **곡선의 x축은 명목 배율이 아니라 실측 객체 중앙값(px)** 이다. 소스마다 원본 해상도가
  달라 같은 imgsz 에서도 11.6px ~ 54.8px 로 퍼진다 — **소스별로 보고한다.**
- **곡선의 점마다 대조군 리샘플링 배율을 함께 적는다.** 640 에서만 1.00배이고,
  320·160 에서는 대조군도 줄어들어 "줄인 것 vs 줄인 것" 비교로 성격이 바뀐다.
  **"진짜 소형에서도 통한다"는 주장은 640 에서만 한다.**
- **소스별 분리 보고 필수.** `rf-screw-nut-bolt` 가 인스턴스의 81% 라 합친 평균만 보면
  그 성격에 가려진다.
- **재현성**: seed 0, deterministic. `batch=-1`(AutoBatch)은 VRAM 여유에 따라 배치가 달라져
  **같은 데이터로 mAP 0.345 vs 0.282** 가 나온 적이 있다(1차 D8 판정 불가의 원인). 배치 고정.
- **채운 마스크는 train 에만 넣는다(D16).** val/test/control 은 손대지 않는다 —
  채점표를 채점 대상이 만들면 채점이 아니다. 학습 라벨은 `annotations.filled.coco.json`,
  평가 라벨은 `annotations.coco.json` 을 쓴다.
- **mask AP 는 전 인스턴스에 마스크가 있는 사진에서만 낸다**(test 217/236장 4,151개,
  val 219/236장 4,201개). 일부만 그려진 사진을 그대로 쓰면 안 그려진 물체를 맞힌 것이 오탐이 된다.
- **대조군은 box AP 로 잰다(D14).** 대조군 8장은 **전부 부분 마스크**(전량 마스크 사진 0장)로 실측 확인됐다. `data/build/control` 472개 중 마스크가 **33개(7.0%)** 뿐이라
  mask AP 를 낼 표본이 안 된다. 대조군의 물음은 "자연 소형을 찾아내는가"라 박스로도 답이 되고,
  마스크 품질은 test(91.5%)·crossdomain(100%)에서 잰다. SAM 으로 채운 마스크 성적은
  **참고치로 병기**하되 주 지표로 쓰지 않는다 — 기준자의 정답이 모델 생성물이면 기준자가 아니다.
- **분할은 촬영 세션 단위로 자른다.** 같은 작업대에서 부품만 재배치해 찍은 사진이 학습과
  평가에 갈리면 실력이 아니라 기억력을 잰다. `rf-arg-fixings3` 는 파일명 접두어가 세션
  단위다(`Photo` 116 / `nuts_bolts_washers_screws` 51 / `PXL` 47 …). 대조군은 전 구간 held-out.

## 모델과 학습 조건 (`docs/proposal.html` 06·07절)

- **주력은 `yolo11-seg`** — n(2.9M/10.5G) · s(10.1M/35.8G) · m(22.4M/124G).
  `yolo26-seg` 는 **기준 크기에서 1회만** 비교하고 **계열 우열은 선언하지 않는다**
  (설정이 YOLO11 기준이라 공정한 비교가 아니다 — 1차 D3 에서 같은 실수를 했다).
  제외: v8/v9-seg(세대·규모), `yolov8-seg-p6`(P6은 큰 물체용, 방향이 반대),
  YOLOE-seg(열린 어휘라 4클래스 고정 과제에 이점 없음), **RT-DETR(분할 설정 자체가 없다)**,
  Mask R-CNN·Mask2Former(툴체인이 갈라진다).
- **가장 촘촘한 헤드가 stride 8 이다.** `yolo11-seg.yaml` 은 `Segment(P3,P4,P5)` 이고
  `mask_ratio: 4` 다. imgsz 640 에서는 객체가 11.6~54.8px 라 괜찮지만
  **imgsz 160 에서 3~14px 가 되어 stride 8 보다 작아진다**(마스크는 1/4 해상도라 3px).
  그래서 **P2(stride 4) 헤드를 실험 축에 넣는다** — 설치본의 p2 설정(`v8`, `26`)은
  **전부 탐지 전용**이라 seg 용 yaml 을 직접 만들어야 한다.
- **증강 결정**: 회전(`degrees`)·상하 뒤집기(`flipud`)를 **켠다**(톱다운이라 방향이 자유롭다 —
  기본값 0 은 사람·차량 전제다). **mixup·cutmix 는 끈다**(이미 겹친 장면에 인위적 겹침을
  더하면 원인 구분이 안 된다). 색·명암 크게 흔들기도 끈다(금속 반사가 재질 단서다).
- **copy-paste 는 혼자 켜면 절반만 적용한 것이다 — 원 논문의 짝을 같이 켠다.**
  분할이라 `copy_paste`·`copy_paste_mode` 가 도구 표준 기능이지만, 그것만으로는 부족하다.
  - **Ghiasi(CVPR 2021)** 는 copy-paste 를 **크게 흔드는 스케일 변형과 짝지을 때** 효과가
    크다고 보고한다. ultralytics `scale` 기본값은 0.5(±50%)로 폭이 좁고, **`(min,max)` 튜플을
    받으므로** 범위를 넓혀 함께 켠다. 붙인 객체가 늘 같은 배율로 들어가면 새로 배울 게 적다.
  - **Kisantal(2019)** 은 두 갈래다 — copy-paste 와 **소형 객체가 담긴 이미지 자체의
    샘플링 비중을 올리는 것.** 뒤엣것을 빼면 절반만 쓴 것이다. 우리 자료는 소스별 객체
    크기 편차가 크고 `rf-screw-nut-bolt` 가 인스턴스의 81% 라, 이 조절이 **쏠림 완화에도
    같이 작용**한다.
  - 근거와 링크는 `docs/proposal.html` 하단 참고 문헌. **기법 이름만 가져오고 조건을 안 가져오면
    원 논문이 보고한 효과가 안 나온다.**
- **화질 열화 증강(노이즈·흐림·압축)은 별도 실험 축**이다. ultralytics 에 이미 있지만
  `A.Blur/MedianBlur p=0.01`, `A.ImageCompression p=0.0` 로 **사실상 꺼져 있다.**
  켜서 재는 것이 실험이고, 효과가 없으면 없다는 사실이 결과다.
- **첫 계획은 10회**: 주 곡선 6(n·s × 640/320/160) + 크기 1(m × 640) + 계열 1(yolo26s × 640)
  + P2 2(s-p2 × 320/160). 결과를 보고 넓힌다.

## 알려진 함정

- **도구는 쓰기 전에 재는 대상으로 먼저 세운다.** 1차 D9 에서 CAM 집중도를 검증 없이 썼다가
  성능과 상관 -0.08 / 정답 박스 면적과 -0.65 로 드러나 폐기했다. **SAM 도 같은 순서를 지켰다** —
  1,706개로 크기별·출처별 IoU 를 재고 나서 채웠다(E2). 이때 **IoU 만 보면 원인을 잘못 짚는다**:
  정밀도·재현율로 방향을 갈라야 "SAM 이 새는가"와 "정답이 거친가"가 구별된다.
- **Roboflow 내보내기는 리사이즈와 증강을 파일에 구워 넣는다.** 받은 즉시 (1) 종횡비 왜곡
  (원형 부품의 w/h 로 확인), (2) 모자이크·색변형 증강 흔적을 검사할 것. `rf-arg-fixings2` 는
  4분할 타일 + 색변형이 구워져 있어 탈락시켰다.
- **학습이 조용히 NaN 으로 발산한다.** fp16 오버플로. ultralytics 는 발산 후에도 계속 돌며
  발산 직전 `best.pt` 를 남겨 "그냥 성능 낮은 런"과 구별이 안 된다. **자동 감지 후 `amp=False`
  재학습**. 1차에서 모델 계열과 무관하게 재현됐다.
- **WSL에서 `nvidia-smi --query-compute-apps` 는 학습 중에도 항상 빈 결과다.** 정지 신호로
  쓰면 오경보가 난다. 진행 감시는 **완료된 epoch 수를 직접 세는** 방식으로.
- **부분 결과로 방향을 선언하지 말 것.** 1차 D5·D6·D9 세 번 다 전체 결과에서 뒤집혔다.
  정정은 지우지 말고 `docs/experiments.md` 에 남긴다.
- **`data/by_resolution/` 은 원본과 하드링크로 묶여 있다.** 한쪽에서 지워도 다른 쪽에 남는다.
  삭제를 반영하려면 양쪽과 주석 json 을 모두 손대야 한다(전례: 4080×3072 에서 35장 삭제).
- **Windows 콘솔 기본 코드페이지가 cp949**라 `—`, `→` 를 print하면 `UnicodeEncodeError`.
  출력 문자열은 ASCII로 쓴다.

## 스코프 밖 (제안하지 말 것)

**항목별 사유는 `docs/proposal.html` 09절 "지금 하지 않는 것과 그 이유" 가 단일 출처다.**
아래는 요약이며, 다시 제안하기 전에 그 절부터 읽을 것. 판단 기준은 "해보면 좋을 것"이 아니라
**"지금 하면 무엇을 알 수 있나"** 이고, 답이 흐려지는 항목은 뒤로 미룬다.

- **포즈 추정(6D pose)·그립 계획.** 자세 정답이 있는 공개 데이터가 하필 2차에서 제외한
  것들과 겹쳐(단일 클래스 또는 GT 비공개), 그걸 쓰면 클래스 축을 잃는다. 분할이 선행 단계다.
- **깊이(RGB-D) 활용.** 채택한 네 세트가 전부 RGB 뿐이다.
- **새로 촬영하거나 사람이 새로 라벨링하는 것.** SAM 의사라벨은 사람 라벨링이 아니라 허용하되,
  **검증을 통과한 뒤에만** 쓴다.
- **3D 렌더링·생성 AI 기반 합성 데이터.** 이 프로젝트가 재려는 게 "실사 화질 vs 인공 화질"의
  격차인데, 렌더·생성 이미지는 그 격차를 하나 더 만들어 측정 대상을 오염시킨다.
- **타일링 추론(SAHI 계열)을 "재학습 없이 얹는" 제안.** 탐지기가 타일 해상도로 학습돼 있다는
  전제 위에 있어 축소 학습과 충돌한다. 쓰려면 타일로 재학습해야 하므로 별도 실험이다.
- **경량화·실시간 처리.** 정확도가 어디서 무너지는지 모르는 상태에서 가볍게 만들면
  성능 하락의 원인이 경량화인지 소형 객체인지 구분되지 않는다. 곡선을 먼저 그린다.
- **원거리 촬영 데이터를 더 찾는 것.** 3라운드에 걸쳐 20종 이상을 검토해 소진했다.
  결론과 후보별 탈락 사유는 `data/rejected/MANIFEST.md` 에 있다 — 다시 찾기 전에 그것부터 읽을 것.
  요지: **부품이 주인공이면 반드시 근접 촬영이고, 부품이 작게 찍혔으면 반드시 다른 것이 주인공이다.**

## 데이터

`data/` 는 git 추적 안 함. 세 폴더로 나뉘고 각각 설명 파일이 있다.

| 경로 | 내용 |
|---|---|
| `data/selected/` | **채택 4종**(원본 형태). `MANIFEST.md` 에 규모·클래스·마스크 비율·처리할 함정 |
| `data/by_resolution/` | 위를 **해상도별로 분리**(하드링크). 그룹마다 해당 주석만 담은 `annotations.coco.json`. `README.md` 에 그룹별 성격 |
| `data/rejected/` | 검토 후 뺀 것(지우지 않고 보류). `MANIFEST.md` 에 실측 근거와 탈락 사유 |

채택 4종 요약 — 합계 **1,812장 / 33,923개**, 전부 CC BY 4.0.

| 데이터 | 장수 | 인스턴스 | 클래스 | 마스크 | 해상도 |
|---|---|---|---|---|---|
| `rf-screw-nut-bolt` | 1,300 | 27,265 | bolt/nut/screw | RLE 전량 | 512×512 **(왜곡, 보정 필요)** |
| `rf-arg-fixings3` | 233 | 5,541 | Bolt/Nut/Screw/Washer | 일부(29%) | 1920×1080 · 4080×3072 · 640×480 · 640×360 |
| `rf-nuts-and-bolts` | 100 | 579 | Bolt/Nut/washer | 폴리곤 전량 | 640×640 |
| `rf-fasteners` | 179 | 538 | bolt/nut | 폴리곤 전량 | 640×640 |

역할 분담 — `rf-screw-nut-bolt` = 마스크 공급원(보정 후), `rf-arg-fixings3` = 평가 기준과
대조군 보유(유일하게 4클래스 + 촬영거리 스펙트럼 + 형상 정상), `rf-nuts-and-bolts` = washer
마스크 71개(전체 washer 마스크의 59%), `rf-fasteners` = 교차 도메인 시험.

**제외할 그룹**: `rf-arg-fixings3/3072x4080`(6장, 주석 0개), `640x360`(12장, 인스턴스 16개).

## 코드·실행 환경

**2차 파이프라인 `scripts/` 는 git 이 추적한다** — `docs/experiments.md` 의 수치가 어떻게 나왔는지
되짚을 수 있어야 해서다. 1차 코드(`sod/`)는 작업 머신에만 있고 추적하지 않는다.

| 스크립트 | 하는 일 |
|---|---|
| `01_build_dataset.py` | 그룹 제외·중복 제거·비율 복원·클래스 통일·세션 단위 분할 → `data/build/` |
| `02_sam_validate.py` | 크기 구간별 층화 표집으로 SAM IoU 측정 (E2) |
| `03_sam_diag.py` | 정밀도·재현율·면적비·정답 꼭짓점 수로 IoU 저하의 원인 분리 (E2-3) |
| `04_sam_washer.py` | washer 구멍 처리와 SAM 모델 3종 비교 (E2-4·5) |
| `05_sam_fill.py` | 관문 통과분만 채워 `annotations.filled.coco.json` 생성 (E3) |

- venv 두 개: **`~/venvs/sod`**(학습 — torch 2.6.0+cu124 / ultralytics 8.4.113 / RTX 4070 12GB),
  **`~/venvs/sod-dl`**(데이터 수집 — `gdown`, `hf`, PIL, numpy, pycocotools). 패키지 관리는 `uv`.
  시스템 파이썬에 pip·ensurepip 이 없고 sudo 도 없어 `uv` 로 우회한 것.
- `.env` 에 `EXA_API_KEY` / `HF_TOKEN` / `ROBOFLOW_API_KEY`. **커밋 금지**(gitignore 됨).
- rar 해제용 `unrar` 를 `~/bin` 에 받아뒀다(시스템에 없음).
- **venv와 변환 산출물은 저장소 밖에 둔다.** 학습은 epoch 마다 이미지를 전부 다시 읽는다.

## git

- 원격이 둘이다. **`origin` = `github.com:hyperion0328/binpicking-seg`(2차, push 대상)**,
  `local` = `/mnt/d/sod-project`(옛 경로). 1차는 별도 저장소
  `github.com:hyperion0328/sod-project` 이고 **거기엔 로컬에 없는 1차 마무리 커밋 32개가 있다** —
  섞지 말 것. push 전에 `git remote -v` 확인.
- `data/`, `runs/`, `outputs/`, `*.pt`, `*.pth`, `.env` 는 추적하지 않는다 — 커밋 전 `git status`.
- 커밋 메시지에는 **무엇을 바꿨는지보다 왜 바꿨는지**를 남긴다.

## 기록 규칙

- 수치를 얻으면 `docs/experiments.md` 에 누적한다 — 버린 시도와 **정정까지** 남긴다.
- 설계 판단을 바꾸면 `docs/proposal.html` 11절 결정 표(D1~D24)에 한 줄 추가한다.
  기법을 새로 들일 때는 **원 논문이 건 조건까지 옮겼는지** `docs/data-notes/references.md` 에서
  확인한다(대외용 `proposal.html` 하단은 v1 형식대로 링크만 싣는다).
  이 표가 "왜 이렇게 했냐"에 대한 단일 출처다.
- `docs/proposal.html` 을 수정하면 아티팩트를 **같은 URL로** 재게시한다:
  <https://claude.ai/code/artifact/8a45abca-5353-4abb-84e3-a05071ef6ccf>
  1차 아티팩트(<https://claude.ai/code/artifact/97ffcc62-6f16-4f06-b65b-0bd728714daf>)는
  `docs/v1/proposal.html` 에 대응하므로 덮어쓰지 말 것.
  (전문 용어 없는 쉬운 말 유지 — 대외용이다.)

## 현재 위치

**기획 ①~⑦ 전부 완료. 학습 21회, 결과 정리까지 끝났다.**

- 결론: **줄여서 만든 소형 객체 곡선은 낙관적 상한이다.** 근접 box 0.814 vs 대조군 0.139~0.162,
  무너지는 문턱 **12px**. 헤드 구조(P2)로도 모델 크기(m)로도 안 메워졌다.
- 통한 것: **P2 헤드**(박스 +9~18%) · **화질 열화 증강**(낯선 도메인 0.553 → 0.795).
- 안 통한 것: 모델 키우기(s → m 개선 없음) · 대비 조작(낯선 도메인을 올리기는 하나 열화 증강의
  3분의 1이고 대조군·test 를 내준다 — "낯선 도메인 손해"는 64ep 기준선 시절 서술이라 틀렸다) ·
  P2 와 마스크 해상도 병용(같은 병목이라 상쇄) ·
  효과가 있던 축 결합(P2+열화): **320 에서** 대조군 이득이 안 쌓임(0.129 → 0.137 → 0.116,
  학습량은 결합이 최다라 교란으로 설명 안 됨). **640 결합은 batch 8 이라 판정 보류** →
  **22회차로 재실행 중**(`yolo11s-p2_640_deg_b16`).
- **batch 16 은 P2@640 에서 들어간다 — 실측했다.** 12GB 중 11.9GB 를 쓰고 OOM 없이 돈다.
  "12GB 에 안 들어간다"는 추측이었고 틀렸다. 다만 **epoch 당 137초로 batch 8(62.5초)의 2.2배**라
  한계에 붙어 있는 대가가 있다(100 epoch 약 3.8시간). 판정은 학습이 끝나야 나온다.
- 문서: `docs/results.md`(기술) · `docs/results.html`(대외용) · `docs/experiments.md`(E1~E11) ·
  `docs/proposal.html`(결정 D1~D24) · `docs/interview.html`(면접 예상 질문 20 — **대외 공개 안 함**,
  약점을 적어 둔 문서다) · `docs/mock-interview.html`(모의면접 도구 — 브라우저로 여는 단일
  HTML, 프로젝트 20 + 기술 기본 20 + 인성·일반 12 문항을 무작위 출제. **대외 공개 안 함**).

**다음에 손댈 것** (`docs/results.md` 6절)
- `screw` 는 결합(P2+열화) 조건에서 test 안 mask AP 가 0.057 → 0.191 로 반응했지만
  대조군은 0.001 → 0.000 으로 그대로다 — 학습 도메인 안에서는 반응하는데 대조군으로는
  안 넘어온다. 가늘고 긴 물체 전용 처방은 여전히 필요.
- `nut` 은 42% 를 찾는데 AP 0.103. 위치는 잡되 정확도·오탐에서 잃는다.
- **조기 종료 기준이 분할 과제와 어긋난다** — mask 정점이 box 정점보다 21~42 epoch 먼저 온다.
- YOLO26 이 대조군 screw 에서 2.9배 높은 이유.
- 열화 증강의 어느 성분이 듣는지 갈라 재기.
- ~~**640 결합을 batch 16 으로 재실행하면 판정할 수 있다**~~ — 재실행 중(22회차).
  끝나면 `docs/experiments.md` E11 과 `docs/results.md` 7절 표, `docs/interview.html` Q11 경고를
  같이 고쳐야 한다(세 곳 다 "시도한 적 없다"로 적혀 있다).

## 이 프로젝트에서 얻은 규칙

측정보다 이쪽이 오래 간다. 근거는 `docs/experiments.md`.

1. **출처를 고정하지 않은 크기 곡선은 읽지 않는다.**
2. **IoU 하나로 원인을 지목하지 않는다** — 정밀도·재현율로 방향을 가른다.
3. **기대한 부수효과는 재보고 적는다.**
4. **전제가 틀렸다는 실측이 나와도, 그 전제에서 나온 처방은 따로 재본다.**
5. **조기 종료 epoch 가 다른 런끼리 비교하지 않는다.**
6. **도구는 조용히 틀린다** — 발산 감지기가 없는 파일을 보고 통과했고, 노이즈 추정기가
   질감을 노이즈로 셌고, `albumentations` 미설치를 ultralytics 가 말없이 건너뛰었다.
7. **정정은 그 정정이 닿는 모든 축에 옮겼는지 확인한다** — E10-1 에서 epoch 를 맞춰 다시
   잰 것이 `deg` 뿐이라 `flat` 판정만 옛 기준선에 남아 방향이 반대로 적혀 있었다.
