# 결과 (2차)

빈피킹용 소형 체결부품 인스턴스 분할. 학습 **18회**, 전 과정 실측은 `experiments.md`.
기획과 판단 근거는 `proposal.html`. 1차는 `v1/`(데이터셋이 완전히 교체돼 **수치는 인용 불가**).

**한 줄** — 줄여서 만든 소형 객체 곡선은 진짜 소형 객체 성능의 **낙관적 상한**이고,
그 격차는 헤드 구조로도 모델 크기로도 메워지지 않았다.

---

## 0. 무엇을 재려 했나

로봇이 상자에서 나사·볼트·너트·와셔를 집으려면 겹쳐 쌓인 더미에서 개체 경계를 픽셀 단위로
갈라내야 한다. 그런데 공개 데이터의 부품 사진은 거의 전부 근접 촬영이다
(`data/rejected/MANIFEST.md`: **부품이 주인공이면 반드시 근접 촬영이고, 부품이 작게 찍혔으면
반드시 다른 것이 주인공이다**). 그래서 이 분야는 보통 **사진을 줄여** 소형 객체를 만든다.

이 프로젝트의 물음은 그 방법이 타당한가다. 답을 내려면 **줄이지 않은 진짜 소형 객체**가
필요했고, 채택한 데이터 안에서 찾았다 — `rf-arg-fixings3` 의 640×480 그룹
(로봇 작업셀 원거리 장면, 물체 중앙값 **11.6px**, 98.3% 가 32px 미만).
이것이 **대조군**이고 학습에 한 번도 넣지 않았다.

---

## 1. 핵심 결과

### 1-1 줄여 만든 곡선은 진짜 소형 객체를 과대평가한다

imgsz 640, YOLO11s, 같은 사진(test 217장)에서:

| | box mAP50-95 | mask mAP50-95 |
|---|---|---|
| test (줄여서 작아진 물체 포함) | **0.8136** | **0.6126** |
| **대조군** (원래 작게 찍힌 물체) | **0.1390** | 잴 수 없음* |

**5.9배 차이.** 320·160 으로 내려가면 격차는 15~200배로 벌어진다.

\* 대조군 24장 중 **모든 인스턴스에 사람이 그린 윤곽이 있는 사진이 0장**이라 mask AP 를 낼
표본이 안 된다. 그래서 대조군은 box AP 로 재고 SAM 채움본 성적을 참고로 병기한다(D14).

**단, 0.139 를 단독 인용하면 안 된다.** 대조군의 정답 박스가 SAM 이 잡은 물체보다 면적
1.5배로 헐겁다(IoU 0.669). 참고 라벨로 재면 0.1616 이다. **참값은 0.139~0.162 구간**이며,
어느 쪽이든 test 의 1/5~1/6 이다.

### 1-2 무너지는 문턱은 12픽셀 부근이다

대조군 472개, YOLO11s @640, conf 0.25 / IoU 0.5:

| 물체 크기 | 정답 | 찾음 | 검출률 |
|---|---|---|---|
| 24px 이상 | 57 | 52 | **91%** |
| 16–24px | 44 | 27 | 61% |
| 12–16px | 114 | 75 | 66% |
| 10–12px | 72 | 35 | 49% |
| 8–10px | 74 | 23 | 31% |
| **8px 미만** | 111 | 13 | **12%** |
| 전체 | 472 | 225 | 48% |

12px 위로는 60~91%, 아래로는 급락한다. 대조군 중앙값이 11.6px 이라 **절반이 문턱 아래**다.

**단, 이것은 크기 축만 본 것이다.** 같은 크기 구간에서 test 와 대조군을 나란히 놓으면
16-24px 에서 95% 대 59% 로 갈린다 — **크기 외의 도메인 요인이 30%p 안팎을 더 깎는다.**
8절에서 갈랐다.

클래스별 검출률 — bolt 71% / nut 42% / washer 31% / **screw 20%**.
가늘고 긴 나사가 원거리에서 사실상 안 잡힌다(대조군 AP 0.005~0.067, 어느 조건에서도 0.07 미만).

### 1-3 윤곽이 네모보다 먼저 무너진다

같은 사진에서 잰 mask/box 비:

| imgsz | box | mask | mask/box |
|---|---|---|---|
| 640 | 0.7900 | 0.6119 | **0.81** |
| 320 | 0.6326 | 0.3196 | 0.51 |
| 160 | 0.4087 | 0.1215 | **0.33** |

물체가 작아질수록 윤곽이 먼저 무너진다. `yolo11-seg` 의 가장 촘촘한 헤드가 stride 8 이고
마스크는 그 1/4 해상도라, imgsz 160 에서 3~14px 물체의 마스크는 격자보다 작아진다.

---

## 2. 시도한 처방과 결과

### 2-1 P2(stride 4) 헤드 — 네모는 살리고 윤곽은 대체재였다

설치본의 p2 설정은 전부 탐지 전용이라 **분할용 yaml 을 직접 만들었다**
(`configs/yolo11s-seg-p2.yaml`). 그리고 ultralytics 검증기가 "마스크 원형은 항상 imgsz/4" 를
상수로 박아둬 P2 에서 죽는 것을 고쳐야 했다(`sodseg/p2_seg.py`).

P2 를 붙이면 원형 해상도가 함께 올라가므로 **2×2 로 짜야 원인이 갈린다.**

| imgsz | 조건 | test box | test mask | 대조군 |
|---|---|---|---|---|
| 160 | 기본 헤드 | 0.4087 | 0.1215 | 0.0017 |
| 160 | **P2** | **0.4820** | 0.1314 | 0.0112 |
| 320 | 기본 헤드 | 0.6326 | 0.3196 | 0.0426 |
| 320 | 기본 + 마스크 해상도 2배 | 0.6784 | 0.3822 | **0.0610** |
| 320 | **P2** | **0.6902** | **0.3832** | 0.0439 |
| 320 | P2 + 마스크 해상도 2배 | 0.6745 | 0.3754 | 0.0216 |

- **네모에서 P2 가 이긴다** — 160 +17.9%, 320 +9.1%. 작을수록 이득이 크다.
- **윤곽에서는 P2 와 마스크 해상도가 같은 병목이다.** 320 에서 각각 +19.9% / +19.6% 인데
  **같이 켜면 오히려 내려간다**(0.3754). 보완이 아니라 대체이므로 **둘 중 하나만** 켠다.
- **대조군은 못 살린다** — 320 에서 0.0426 → 0.0439. 구조로 메워지지 않는다.

비용: 파라미터 +1.6%, 학습 시간 320 에서 +36%.

### 2-2 모델 크기 — 키운다고 되지 않는다

| 모델 | 파라미터 | test box | test mask | 대조군 |
|---|---|---|---|---|
| YOLO11 n | 2.9M | 0.7594 | 0.5766 | 0.1014 |
| **YOLO11 s** | 10.1M | **0.8136** | **0.6126** | **0.1390** |
| YOLO11 m | 22.4M | 0.8031 | 0.5976 | 0.1276 |

n → s 는 오르고 **s → m 은 개선이 없다.** (m 은 77 epoch 에서 조기 종료해 epoch 가 다르므로
"m 이 나쁘다"가 아니라 "키워도 개선이 없다"까지만 말한다.)

### 2-3 화질 열화 증강 — 통했지만 이유가 달랐다

기획서는 "줄인 사진은 실제보다 깨끗하니 노이즈·흐림·압축으로 되돌린다"를 근거로 삼았다.
**물체 크기를 12px 로 맞춰 재보니 그 전제는 성립하지 않았다** — 노이즈는 축소본이 오히려
더 크고(4.49 vs 3.21), 가장 큰 차이는 **대비**였다(미헬슨 0.588 vs 0.255).

그래도 원안을 버리지 않고 실측이 가리킨 처방(`flat`, 대비 축소)과 **함께** 돌렸다.

| 조건 (640, 100 epoch) | test box | test mask | 대조군 | 교차 도메인 |
|---|---|---|---|---|
| 기준선 | 0.8136 | 0.6126 | 0.1390 | 0.5525 |
| **`deg` 노이즈·흐림·압축** | 0.8133 | **0.6224** | **0.1480** | **0.7952** |
| `flat` 대비 축소 | 0.7900 | 0.6093 | 0.1325 | 0.6291 |
| `both` | 0.8140 | 0.6087 | 0.1112 | 0.8085 |

- **원안이 이기고 실측에서 나온 처방이 졌다.** `flat` 은 대조군에서 잡음 범위로 오르고
  교차 도메인은 떨어진다.
- **`deg` 의 값은 과적합 억제다.** 100 epoch 까지 학습하면 기준선의 교차 도메인이
  0.7104(64ep) → **0.5525** 로 무너지는데 `deg` 는 0.7952 를 지킨다(**+43.9%**).
  대조군 이득은 +6.5% 로 작다.
- 가장 안 잡히던 screw 가 대조군에서 0.018 → **0.067** 로 올랐다.

**반박된 것은 설명이었지 처방이 아니었다.** 전제를 실측으로 뒤집었다고 처방까지 갈아탔으면
이 축을 통째로 놓쳤다.

### 2-4 계열 비교 — 우열은 선언하지 않는다

| 모델 | test box | test mask | 대조군 |
|---|---|---|---|
| YOLO11 s | 0.8136 | 0.6126 | 0.1390 |
| YOLO26 s | 0.7969 | 0.6084 | 0.1316 |

학습 조건이 YOLO11 기준이라 공정한 비교가 아니다(D10). 1차 D3 에서 같은 실수를 했다.
다만 **대조군의 screw 에서 YOLO26 이 0.052 로 YOLO11 의 0.018 보다 2.9배 높다** —
가장 안 잡히는 클래스라 다음 실험거리로 남긴다.

---

## 3. 데이터 쪽에서 나온 것

### 3-1 SAM 은 쓰기 전에 재는 대상으로 먼저 세웠다

정답 윤곽이 있는 1,706개에 정답 박스를 프롬프트로 주고 IoU 를 쟀다.
**합친 0.814 를 그대로 읽었으면 방향을 잘못 잡았다** — 출처별로 가르니
`rf-nuts-and-bolts` 0.910 / `rf-screw-nut-bolt` 0.824 / `rf-arg-fixings3` **0.700** 이었다.

낮은 쪽을 파보니 **정밀도 0.97 / 재현율 0.70** 이었다. SAM 이 새는 게 아니라 정답 안쪽에
더 좁게 들어가 있었고, 그 정답은 꼭짓점 9개짜리 거친 폴리곤이었다. **낮은 IoU 는 SAM 이
아니라 정답 탓**이었고 겹쳐 그린 그림으로 확인했다.

→ 채우기로 결정. **학습용에만** 2,536개(97.3% 통과), 시험지와 대조군은 손대지 않았다.
와셔는 정답이 100% 구멍을 메운 원판이라 SAM 의 도넛을 메워 관례를 맞췄다(IoU 0.834 → 0.903).

### 3-2 정답 품질이 점수의 상한을 정한다

사람이 그린 윤곽의 꼭짓점 수 중앙값 — `rf-arg-fixings3` **9** / `rf-nuts-and-bolts` 14 /
`rf-fasteners` 29~93 / `rf-screw-nut-bolt` 화소 단위.

그 결과 640 에서 출처별 mask AP 가 **0.038 ~ 0.775** 로 벌어진다.
합산 0.61 은 **어느 것도 대표하지 않는다.** 소스별 병기가 필수다.

### 3-3 데이터를 골라 붙이는 과정에서 걸린 것들

- **`rf-screw-nut-bolt` 는 종횡비가 왜곡돼 있었다** — 너트 가로세로비 0.59(원형이면 1.0).
  Roboflow 의 `Stretch to 512×512` 흔적. 512×288 로 복원해 1.06 을 확인하고 학습에 넣었다.
- **분할은 촬영 세션 단위로 잘랐다.** 무작위로 나누면 1~2초 간격 연속 프레임이 학습과 평가로
  갈려 실력이 아니라 기억력을 재게 된다.
- **주석 0개 사진은 지우지 않았다.** 공구만 있는 배경 사진이라 오탐을 줄이는 재료다.

---

## 4. 판단이 뒤집힌 기록

결과보다 이쪽이 이 프로젝트의 산출물에 가깝다. 전부 `experiments.md` 에 근거가 있다.

| # | 처음 판단 | 실측 | 교훈 |
|---|---|---|---|
| 1 | SAM IoU 가 크기와 무관하다 | 출처가 섞여 있었다 | **출처를 고정하지 않은 크기 곡선은 읽지 않는다** |
| 2 | `arg-fixings3` 에서 SAM 이 나쁘다 | 나쁜 건 정답이었다 | **IoU 만으로 원인을 지목하지 않는다. 정밀도·재현율로 방향을 가른다** |
| 3 | 소형 사진 오버샘플링이 쏠림도 완화한다 | 82.9% → 84.8% 로 오히려 늘었다 | 서술 철회. 기법은 유지 |
| 4 | 줄인 사진은 실제보다 깨끗하다 | 노이즈는 반대 방향, 차이는 대비였다 | **전제가 틀려도 그 전제에서 나온 처방은 따로 재본다** |
| 5 | 열화 증강이 대조군을 +17% 올린다 | epoch 를 맞추니 +6.5% | **조기 종료 epoch 가 다른 런끼리 비교하지 않는다** |

도구 쪽에서 걸린 것 — 발산 감지기가 상대 경로 탓에 없는 파일을 보고 **조용히 통과**했고,
전역 라플라시안 노이즈 추정기는 **나사산 질감을 노이즈로 셌으며**,
`albumentations` 가 설치돼 있지 않아 12회 학습에 화질 증강이 **하나도 걸린 적이 없었다**
(ultralytics 는 이를 조용히 건너뛴다).

---

## 5. 지금 시점의 권고

| 물음 | 답 |
|---|---|
| 어떤 모델 | **YOLO11s-seg**. n 은 부족하고 m 은 개선이 없다 |
| 어떤 헤드 | 물체가 격자보다 작아지는 구간에서 **P2**. 네모 +9~18% |
| 윤곽 해상도 | P2 를 쓰면 그대로, 안 쓰면 **마스크 해상도 2배**. **같이 켜지 않는다** |
| 증강 | **노이즈·흐림·압축을 켠다.** 과적합을 막아 낯선 도메인을 지킨다 |
| 대비 조작 | **하지 않는다.** 대조군 이득은 잡음 범위인데 교차 도메인을 잃는다 |
| 학습 길이 | 100 epoch 는 test 를 올리지만 **교차 도메인을 무너뜨린다.** 열화 증강과 함께 쓴다 |
| 성능 기대치 | **16px 이상**이면 검출률 93~95% · 윤곽 IoU 0.48~0.57 로 쓸 만하다. **12~16px** 은 찾기는 84% 하지만 윤곽 IoU 0.254 라 집을 지점을 못 정한다. **12px 미만**은 학습 도메인에서도 58%, 낯선 원거리에서는 34% 다 |

---

## 6. 남은 것

- **`screw` 는 어느 조건에서도 대조군 AP 0.07 을 못 넘는다.** 가늘고 긴 물체 전용 처방이 필요하다.
- **`nut` 은 42% 를 찾는데 AP 는 0.103 이다.** 위치는 잡되 정확도나 오탐에서 잃는다 — 따로 볼 것.
- **조기 종료 기준이 분할 과제와 어긋난다.** box 정점보다 mask 정점이 21~42 epoch 먼저 온다.
  `best.pt` 는 box 가 섞인 fitness 로 고르므로 저장 시점 mask 가 최고보다 5~13% 낮다.
- **YOLO26 이 대조군 screw 에서 2.9배 높은 것**의 원인.
- **열화 증강의 어느 성분이 듣는가** — 노이즈·흐림·압축을 갈라 재지 않았다.
- 대조군이 24장뿐이라 표본이 작다. 같은 성격의 원거리 데이터를 더 얻으면 결론이 단단해진다.

---

## 7. 모델별 전체 비교

18회 전부. 통일 잣대(입력 해상도 그대로), test 는 사람 윤곽 전량인 217장.

| 모델 | imgsz | ep | box50 | box50-95 | mask50 | mask50-95 | 대조군 box50 | 대조군 50-95 | 교차 mask50 |
|---|---|---|---|---|---|---|---|---|---|
| **s · 640 · 열화증강** | 640 | 100 | 0.976 | 0.813 | **0.963** | **0.622** | **0.418** | **0.148** | 0.897 |
| s · 640 | 640 | 100 | 0.977 | 0.814 | 0.961 | 0.613 | 0.377 | 0.139 | 0.736 |
| s · 640 (64ep 조기종료) | 640 | 64 | 0.970 | 0.790 | 0.952 | 0.612 | 0.323 | 0.126 | 0.870 |
| s · 640 · 대비축소 | 640 | 95 | 0.967 | 0.790 | 0.949 | 0.609 | 0.329 | 0.133 | 0.834 |
| s · 640 · 둘 다 | 640 | 100 | 0.977 | 0.814 | 0.956 | 0.609 | 0.328 | 0.111 | **0.920** |
| YOLO26 s · 640 | 640 | 100 | 0.966 | 0.797 | 0.952 | 0.608 | 0.345 | 0.132 | 0.688 |
| m · 640 | 640 | 77 | 0.954 | 0.803 | 0.937 | 0.598 | 0.321 | 0.128 | 0.822 |
| n · 640 | 640 | 100 | 0.942 | 0.759 | 0.919 | 0.577 | 0.267 | 0.101 | 0.853 |
| s+P2 · 320 | 320 | 95 | 0.945 | 0.690 | 0.831 | 0.383 | 0.137 | 0.044 | 0.736 |
| s · 320 · 마스크2x | 320 | 88 | 0.930 | 0.678 | 0.842 | 0.382 | 0.167 | 0.061 | 0.857 |
| s+P2 · 320 · 마스크2x | 320 | 78 | 0.942 | 0.675 | 0.834 | 0.375 | 0.060 | 0.022 | 0.779 |
| s · 320 | 320 | 78 | 0.886 | 0.633 | 0.731 | 0.320 | 0.129 | 0.043 | 0.753 |
| n · 320 | 320 | 95 | 0.837 | 0.573 | 0.675 | 0.284 | 0.037 | 0.012 | 0.851 |
| s+P2 · 160 | 160 | 100 | 0.819 | 0.482 | 0.451 | 0.131 | 0.033 | 0.011 | 0.617 |
| s · 160 | 160 | 83 | 0.760 | 0.409 | 0.404 | 0.121 | 0.009 | 0.002 | 0.585 |
| s · 160 · 마스크2x | 160 | 88 | 0.701 | 0.398 | 0.381 | 0.109 | 0.044 | 0.014 | 0.683 |
| s+P2 · 160 · 마스크2x | 160 | 78 | 0.756 | 0.451 | 0.395 | 0.100 | 0.010 | 0.003 | 0.659 |
| n · 160 | 160 | 78 | 0.665 | 0.325 | 0.268 | 0.072 | 0.009 | 0.003 | 0.577 |

**`mAP50` 과 `mAP50-95` 를 반드시 함께 본다.** 640 학습 런은 전부 mask50 이 0.92~0.96 인데
mask50-95 는 0.58~0.62 다. 앞엣것만 인용하면 "거의 완벽하다"로 읽히고, 뒤엣것만 인용하면
실제보다 나쁘게 읽힌다.

---

## 8. 크기 구간별 성능 — "소형에 강한가"에 대한 직접적인 답

`scripts/12_size_breakdown.py`. conf 0.25 · 박스 IoU 0.5 · 640 환산 크기.
**mAP 합산으로는 이 물음에 답할 수 없다** — test 인스턴스의 12px 미만이 2% 뿐이고
42% 가 32px 이상이라, 전체 mask50 0.96 은 주로 16px 이상에서 얻은 점수다.
대조군은 반대로 **55% 가 12px 미만**이다.

`s · 640 · 열화증강` (최고 모델):

| 크기 | test 정답 | test 검출률 | test 윤곽 IoU | 대조군 정답 | 대조군 검출률 |
|---|---|---|---|---|---|
| 0-8px | 3 | 33% | - | 111 | **21%** |
| 8-12px | 107 | **58%** | - | 146 | **34%** |
| 12-16px | 303 | 84% | **0.254** | 114 | 54% |
| 16-24px | 1,606 | 95% | 0.475 | 44 | 59% |
| 24-32px | 690 | 93% | 0.564 | 49 | 94% |
| 32px+ | 1,990 | 94% | 0.572 | 8 | 100% |
| **전체** | 4,699 | **93%** | | 472 | **46%** |

### 8-1 읽는 법

**(1) 네모 찾기는 16px 이상에서 안정적이다** — test 93~95%, 대조군도 24px 이상은 94~100%.

**(2) 12px 미만에서는 test 에서도 무너진다** — 8-12px 구간이 **58%** 다.
학습 데이터와 같은 성격의 사진에서도 절반 남짓만 찾는다.

**(3) 찾아도 윤곽이 안 맞는다.** test 기준 윤곽 IoU 가 12-16px 에서 **0.254**,
32px+ 에서 0.572 다. 빈피킹은 집을 지점을 정해야 하므로 윤곽이 필요한데,
**16px 미만에서는 윤곽이 실용 수준이 아니다.**

**(4) 정정 — "12px 문턱"은 절반만 맞았다.** 같은 크기 구간에서도 test 와 대조군이 갈린다:

| 크기 | test | 대조군 | 차이 |
|---|---|---|---|
| 8-12px | 58% | 34% | 24%p |
| 12-16px | 84% | 54% | 30%p |
| 16-24px | 95% | 59% | **36%p** |

크기만이 원인이면 두 값이 같아야 한다. **크기 외에 도메인 요인이 30%p 안팎을 더 깎는다** —
대조군은 공구가 섞이고 대비가 낮고 배경이 어수선하다. 앞서 "문턱은 12px"라고만 적은 것은
**크기 축만 본 것이고, 도메인 축이 비슷한 크기의 몫을 차지한다.**

**(5) 대조군의 윤곽 IoU(0.59~0.68)가 test 보다 높은 것은 읽지 않는다.**
대조군의 정답 마스크는 33개(7%)뿐이고 전부 꼭짓점 9개짜리 거친 폴리곤이라
**헐거운 정답이 맞추기 쉬운** 것뿐이다(E2-3).

### 8-2 모델 크기의 효과도 크기 구간에서 갈린다

| 크기 | `n · 640` 대조군 | `s · 640 · 열화` 대조군 |
|---|---|---|
| 0-8px | 14% | 21% |
| 8-12px | 25% | 34% |
| 12-16px | 46% | 54% |
| 전체 | 39% | 46% |

작은 구간일수록 상대 이득이 크지만(14% → 21%), **절대 수준은 여전히 낮다.**

### 8-3 그래서 "소형 부품 탐지를 잘하는가"

- **16px 이상**: 잘한다. 검출률 93~95%, 윤곽 IoU 0.48~0.57.
- **12~16px**: 찾기는 84% 하는데 **윤곽이 0.254** 라 집을 지점을 정하기 어렵다.
- **12px 미만**: 학습 도메인에서도 58%, 낯선 원거리에서는 34% 다. **못 한다.**

**결론 — 이 모델은 "근접~중거리 체결부품 분할"은 매우 잘하고, "원거리 소형 탐지"는 아직 아니다.**
그리고 그 격차를 만든 것이 크기만이 아니라 **크기와 도메인이 반씩**이라는 것이 이번 측정의 결과다.

---

## 9. 실행 방법

```
scripts/01_build_dataset.py   전처리 -> data/build/
scripts/02_sam_validate.py    SAM 크기별·출처별 IoU
scripts/03_sam_diag.py        IoU 저하의 원인 분리(정밀도·재현율)
scripts/04_sam_washer.py      와셔 구멍 처리와 SAM 모델 비교
scripts/05_sam_fill.py        관문 통과분만 채우기
scripts/06_to_yolo.py         COCO -> YOLO-seg (이미지 복제 없음)
scripts/07_train.py           학습 (--cfg P2 / --mask-ratio / --degrade / --flatten)
scripts/08_eval.py            평가 (출처별 분리, 통일 잣대)
scripts/09_imgstats.py        사진 전체 화질 통계
scripts/10_quality_gap.py     물체 크기를 맞춘 화질 비교
scripts/11_visualize.py       정답/예측 겹쳐 보기 · 놓친 것 · 조건 비교
```

환경: `~/venvs/sod` (torch 2.6.0+cu124 / ultralytics 8.4.113 / RTX 4070 12GB).
전 과정 재현: seed 0, deterministic, batch 16 고정.
